services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: umiurl
      POSTGRES_USER: umiurl
      POSTGRES_PASSWORD: umiurl
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
